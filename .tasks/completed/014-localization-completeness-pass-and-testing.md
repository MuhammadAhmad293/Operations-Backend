# 014 – Localization Completeness Pass & Testing

## Status

`completed`

## Goal

Close out BR-20 across every module built in phases 005–013, implement the priority test list
from `meezan-backend.md` §7, and fold every planning-session correction back into
`.github/meezan-spec.md`.

## Context

Final phase of the full Meezan backend implementation plan approved on 2026-08-07. Full
technical detail lives in the approved plan at
`C:\Users\lenovo\.claude\plans\we-are-starting-a-lively-pudding.md`.

Covers: BR-20 (final sweep); `meezan-backend.md` §7 (all 6 priorities).

## Sub-Tasks

| #   | Description                                                                                                         | Status     |
| --- | ----------------------------------------------------------------------------------------------------------------- | ---------- |
| 1   | Audit every localized key added in phases 006–013 has both `en`/`ar` entries and is actually used                 | ✅ done |
| 2   | Unit tests: karat conversion table                                                                                 | ✅ done |
| 3   | Unit/integration tests: Zakat state machine incl. Hijri boundary dates                                            | ✅ done |
| 4   | Integration test: fee cascade on parent transaction delete                                                        | ✅ done |
| 5   | Integration test: soft-delete dropdown semantics (Wallet + Category)                                              | ✅ done |
| 6   | Unit test: cross-currency transfer math with overridden rate                                                      | ✅ done |
| 7   | Integration test: rate job normalization, append-only, fallback, circuit-breaker                                  | ✅ done |
| 8   | Update `.github/meezan-spec.md` to fold in the Live Spec Correction + Round-2 refinement so it matches the shipped implementation | ✅ done |

## Notes

- Sub-task 1 (2026-08-09): full BR-20 audit, scoped to the plan's own phases 005–013 (Phase 001–004 — Auth, Email, Refresh Tokens — predate this plan entirely, per the plan document's own framing, so are out of this sub-task's stated boundary; noted separately below since the audit surfaced a real gap there too). Scripted checks, no code changes.
  - **3-file parity** (`ILocalizationService.cs`/`LocalizationService.cs`/`localizationFile.json`): all 48 keys present in all three files, zero duplicates, every entry has both `en` and `ar` values. Clean (this repeats Phase 012 sub-task 9 and Phase 013 sub-task 3's checks now that no more keys have been added since).
  - **Every key has a real `Localization.<Key>` call site** (grepped every `.cs` file under `Meezan.Services`/`Meezan` except the two definition files): 46 of 48 keys are referenced; the 2 that aren't are both already-documented, intentional exceptions from prior phases, not new findings — `ZakatPaymentDeletionWarning` (Phase 009: by design, consumed by the frontend's delete-confirmation UI, not backend code) and `PasswordPolicyViolation` (Phase 002, see below).
  - **Raw hardcoded strings bypassing `Localization.*`**: grepped every `throw new *Exception("literal")` across `Meezan.Services`/`Common`. Within this sub-task's actual scope (phases 006–013, i.e. everything this plan itself built), the only hits are two `HttpRequestException`s in `FrankfurterRateProvider.cs` (Phase 010) — confirmed **not** a BR-20 violation: `RateService.GetLatestAsync` (the only path live API requests take) never calls a provider directly at all — per BR-19, providers are only ever invoked from the background `SyncAsync` job — so these messages are strictly internal diagnostics (caught by `CompositeRateProvider`'s fallback chain, or logged by Hangfire on total failure) and never reach a client response.
  - **Found, not fixed (out of this sub-task's stated phases-006–013 scope — Phase 001/002, pre-dates this plan)**: `AuthService.cs`/`UserService.cs` contain roughly a dozen raw hardcoded English exception messages (`"User not found"`, `"Session not found"`, `"First name is required"`, `"Password is required"`, etc.) that bypass localization entirely — an `Accept-Language: ar` request hitting any of these still gets English text, a genuine BR-20 violation. Additionally, `ValidatorHelper.ValidatePasswordPolicy` (`Common/Validator/ValidatorHelper.cs`) returns its own hardcoded English `ErrorMessage` strings, and `AuthService` throws that raw message directly (`Meezan.Services/AuthService/AuthService.cs:307`) instead of the already-existing, fully-wired-but-orphaned `Localization.PasswordPolicyViolation` key (added in Phase 002 apparently for exactly this purpose, then never actually connected). Left unfixed here since it's pre-existing Phase 001/002 code outside this plan's own module boundary (same scope discipline used throughout phases 005–013, e.g. Phase 012 sub-task 1's decision not to touch already-shipped `TransactionService` for an unrelated gap) — flagged here for the user's awareness/prioritization rather than fixed silently.
  - `dotnet build Meezan.sln` clean, 0 errors. No code changes this sub-task — audit only.

- Sub-task 2 (2026-08-09): created the solution's first test project, `Meezan.Tests` (xUnit — `dotnet new xunit`, added to `Meezan.sln`, references `Common.csproj`). No test project existed anywhere in this repo before this sub-task.
  - **Extracted `TransactionService.ComputeKarat`'s pure arithmetic into `Common/GoldPurity/IGoldPurityCalculator`/`GoldPurityCalculator`** (`ValidKarats`, `IsValidKarat`, `ToPureGoldGrams = amount × karat ÷ 24`) so it could be unit-tested directly rather than via reflection into a private method or a full DI-mocked `TransactionService`. This is a minimal, behavior-preserving extract-method refactor — `TransactionService.ComputeKarat` keeps its currency-code branching and exception-throwing exactly as before, just delegating the formula/validity-check to the new class — justified because this sub-task's own job is "write unit tests for this logic," and the alternative (reflection) is worse practice for a deliverable meant to demonstrate the codebase's test quality bar. DI-registered in `CommonResolver.cs` alongside the other `Common/` helpers (`IHijriCalendarHelper`, `IValidatorHelper`, etc. — same established pattern).
  - **`ToPureGoldGrams` deliberately does not round** — matches this codebase's consistent, established convention (confirmed repeatedly across Phase 012) of never rounding in C#, always leaving `decimal(18,3)` column precision as the single source of truth for stored-value rounding. Tests assert the raw formula's mathematical correctness *and separately* that `Math.Round(raw, 3)` matches spec §3.2's own worked-example table (`10g` at each of the five karats → `10.000/9.167/8.750/7.500/5.833`), rather than asserting rounding behavior the production code doesn't itself perform.
  - **17 tests, all passing** (`dotnet test Meezan.Tests`): the 5-karat worked example (raw-then-rounded), 24K passthrough (no purity loss), zero-amount edge case, all 5 valid karats accepted, 5 representative invalid values (`23, 20, 10, 0, -24`) rejected.
  - **Live regression-verified the refactor via a real HTTP transaction** (10g @ 22K into a real `GOLD` wallet) after restarting the app: `PureGoldGrams = 9.167` in the database — exactly matching both the spec's worked example and the new unit test, confirming the DI wiring and the refactored `ComputeKarat` still produce identical production behavior. Test data cleaned up afterward; no `Program.cs` changes were made.
  - `dotnet build Meezan.sln` clean, 0 errors; `dotnet test Meezan.Tests` — 17/17 passing.

- Sub-task 3 (2026-08-09): `Meezan.Tests/ZakatEngine/` — 10 integration tests exercising the **real, unmodified** `ZakatEngine`/`ZakatPotCalculator` (Phase 012) against an isolated `Microsoft.EntityFrameworkCore.InMemory`-backed `AppDbContext` per test (added the package to `Meezan.Tests`; added a project reference to `Meezan.Services`). Only `IRateService` is a test double (`FakeRateService` — a simple configurable-rate dictionary; rate-integration behavior itself is sub-task 7's concern, not the state machine's).
  - **`ZakatEngineTestFixture`**: builds a fresh named InMemory database per test (full isolation, no shared state), wires the real `UnitOfWork`/repositories/`ZakatEngine`/`ZakatPotCalculator`/`HijriCalendarHelper`, and provides seeding helpers (funded account + SAR wallet, backdating a cycle's `HawlDueHijri`, seeding a `Due` cycle with linked Zakat transactions).
  - **A real, deliberate scope boundary, not an oversight**: EF Core's InMemory provider doesn't enforce `decimal(18,3)` column rounding the way SQL Server does, so this suite tests state-machine *transitions* (nisab crossing, status changes, cycle creation, Hijri-date comparisons) — precision/rounding correctness is already covered by sub-task 2's karat-conversion unit tests and Phase 012's extensive live SQL-Server verification.
  - **Caught the exact same class of bug found and fixed in Phase 012 sub-task 10, this time in the test harness itself**: `ReevaluateAsync`/`RecomputeCyclePaymentAsync` only ever *stage* their `Create`/`Update` calls — every real caller persists them via `UnitOfWork.ExecuteInTransactionAsync`'s final `SaveChangesAsync`. Calling them bare in a test (no wrapping transaction, no explicit flush) silently drops every cycle mutation, so all 6 tests exercising cycle creation/transitions initially failed. **Not a production bug** — `ExecuteInTransactionAsync` needs a relational provider (`BeginTransactionAsync`) InMemory doesn't support anyway, so the fixture instead adds thin `ReevaluateAsync`/`RecomputeCyclePaymentAsync` wrapper methods that call the engine then `UnitOfWork.CommitAsync()` explicitly — documented in the fixture's own comments so this doesn't get miscopied as a pattern elsewhere. Confirmed the fix by re-running: all 10 tests passed once every call site used the wrapper.
  - **Coverage**: nisab reached → `Active` cycle created (`HawlStartHijri`/`HawlDueHijri` exactly right); pot stays below nisab → no cycle; pot drops below nisab → `Active → Broken`; re-funding after `Broken` → a **new** `Active` cycle (the `Broken` one never resumes) — the "conditional new Active" case; **Hijri boundary**: one day before `HawlDueHijri` → stays `Active` (no premature transition), exactly on `HawlDueHijri` → transitions to `Due` with the correct `PotGoldGramsAtDue`/`ZakatDueGoldGrams` (2.5%) and an unconditional new `Active` cycle; two consecutive unpaid years cascading into two simultaneous `Due` cycles + one `Active` (automates Phase 012 sub-task 3's manually-verified scenario as a regression test); `RecomputeCyclePaymentAsync` → `Paid` once linked transactions cover the due amount, stays `Due` on partial coverage, and correctly counts `ExternalPaidGoldGrams` toward the total.
  - `dotnet build Meezan.sln` clean, 0 errors; `dotnet test Meezan.Tests` — 27/27 passing (17 from sub-task 2 + 10 new).

- Sub-task 4 (2026-08-09): `Meezan.Tests/TransactionService/` — 4 integration tests against the **real, unmodified** `TransactionService.Add`/`.Delete` (Phase 009). Unlike sub-task 3's `ZakatEngine` suite, `Add`/`Delete` run their work inside `UnitOfWork.ExecuteInTransactionAsync`, which needs `Database.BeginTransactionAsync` — a relational-only feature EF Core's InMemory provider doesn't support — so this suite uses **SQLite in-memory** instead (`Microsoft.EntityFrameworkCore.Sqlite`, `Data Source=:memory:` with the connection kept open for the database's lifetime), giving real transaction semantics while staying fast and fully isolated.
  - **Hit and fixed two setup-only obstacles, no production code changes**:
    1. `MapsterMapper.ServiceMapper`'s constructor needs an `IServiceProvider` (used for DI-resolved custom mappings, none of which this suite needs) — built with an empty `ServiceCollection().BuildServiceProvider()`.
    2. Every `EntityConfiguration` uses SQL Server's `HasDefaultValueSql("GETDATE()")` for `CreationTime`/`LastModificationTime`, which SQLite has no equivalent for and fails schema creation outright. Fixed by registering `GETDATE` as a custom SQLite function on the connection (`SqliteConnection.CreateFunction("GETDATE", () => DateTime.UtcNow)`) — the standard technique for running a SQL-Server-authored EF model against SQLite in tests.
  - Added `Common/GoldPurity`'s real `GoldPurityCalculator` (no fake needed, already pure), plus new shared test doubles in `Meezan.Tests/TestSupport/`: `FakeLocalizationService` (the real `LocalizationService` resolves its JSON file relative to `Assembly.GetEntryAssembly().Location` — the test host's location under `dotnet test`, not this project's, so it can't be used as-is; each of the 48 properties just echoes its own name) and `FakeFileStorageService` (unused by these tests, exists only because the constructor requires one). Moved `FakeRateService` from the `ZakatEngine` folder into `TestSupport` since it's now shared across two suites.
  - **Coverage**: `Add` with a `Fee` creates both the parent and a linked fee `Transaction` (`IsFee=true`, `ParentTransactionId` pointing at the parent); `Delete` removes **both** rows (true hard delete, not soft — confirmed via `BaseRepository.Delete`'s `Remove()`, matching BR-09's note that the fee→parent FK is `Restrict`, not `Cascade`, so `TransactionService.Delete` must find and delete the fee explicitly); wallet balance is exactly restored (parent + fee amounts both reversed); deleting one transaction+fee pair leaves a second, unrelated pair completely untouched.
  - **Doubles as a regression test for the `ExecuteInTransactionAsync` row-count fix** (this phase's sub-task-adjacent Phase 012 sub-task 11 finding): `Delete`'s `result > 0 ? Success : Error` check depends on that fix to report success correctly whenever `ZakatEngine.ReevaluateAsync` (called inside the same transaction) makes no further changes of its own — exactly the scenario these tests exercise, and `deleteResult.Status == Success` passing confirms it holds for `TransactionService.Delete`, not just the `JobService` call site the bug was originally found in.
  - `dotnet build Meezan.sln` clean, 0 errors; `dotnet test Meezan.Tests` — 31/31 passing (27 prior + 4 new).

- Sub-task 5 (2026-08-09): `Meezan.Tests/TransactionService/SoftDeleteDropdownTests.cs` — 10 integration tests against the **real, unmodified** `WalletService.Delete`/`.GetAll`, `CategoryService.Delete`/`.GetTree`, and `TransactionService.GetById`/`.Update` (BR-06). Extended the sub-task 4 SQLite fixture with `WalletService`/`CategoryService` instances (trivial constructors, no new dependencies needed) rather than building a separate fixture.
  - **Coverage, Wallet**: hard-deletes when unreferenced by any transaction; soft-deletes (`IsDeleted=true`, row still present) when a transaction references it; `GetAll` (the dropdown) excludes the soft-deleted wallet; `TransactionService.GetById` on the referencing transaction still resolves the soft-deleted wallet's name (BR-06's "historical transactions keep displaying it").
  - **Coverage, Category**: the same three (hard-delete unreferenced / soft-delete when transaction-referenced / excluded from `GetTree`), **plus** a case Wallet doesn't have — a category is also "referenced" (soft-delete, not hard) purely by having live children, even with zero transactions, since hard-deleting a parent would violate the self-referencing FK (`Restrict`); confirmed via a manually-seeded child category.
  - **The full BR-06 lifecycle end-to-end**: soft-delete a transaction's wallet, then `TransactionService.Update` that same transaction onto a *different* wallet, then confirm (a) the dropdown still excludes the old, now-orphaned wallet and (b) the transaction now resolves the *new* wallet's name — the "once reassigned, the deleted value is gone for good" acceptance criterion from UC-07, verified as a natural consequence of the dropdown-exclusion behavior rather than a separately-coded rule (matching how the production code actually implements it — there's no special "can't go back" check anywhere, it just falls out of `GetByAccountAsync`'s `!IsDeleted` filter never including it again).
  - `dotnet build Meezan.sln` clean, 0 errors; `dotnet test Meezan.Tests` — 41/41 passing (31 prior + 10 new).

- Sub-task 6 (2026-08-09): same extraction pattern as sub-task 2's `GoldPurityCalculator` — `TransactionService.ResolveConversion`'s BR-10 override math was already a dependency-free pure function (only the "is this even a cross-currency transfer" check needed the `Wallet` entities), so it moved to `Common/CrossCurrencyConversion/ICrossCurrencyConversionResolver`/`CrossCurrencyConversionResolver`, DI-registered in `CommonResolver.cs`. `TransactionService.ResolveConversion` is now a 2-line instance method that decides cross-currency-or-not from the wallets, then delegates the actual math — same behavior-preserving-refactor rationale as sub-task 2 (this sub-task's job is to make the logic testable; a 3rd new constructor dependency added to `TransactionService` this phase, after `IGoldPurityCalculator`).
  - **7 tests** (`Meezan.Tests/CrossCurrencyConversion/CrossCurrencyConversionResolverTests.cs`): not-a-transfer → nothing; cross-currency with no override → nothing (live lookup is the caller's job); **UC-14's exact worked example** (USD→SAR, 1,000 @ rate 3.75 → rate `3.75`, converted `3750`); converted-amount override instead → derives the effective rate (`3800/1000 = 3.8`); both overrides supplied → the converted amount wins (it's what actually lands in the destination wallet, so it's authoritative over a possibly-stale rate); amount `0` with a converted-amount override → divide-by-zero guard returns a `null` rate.
  - **Live regression-verified the exact UC-14 acceptance criterion** through a real HTTP transfer after restarting the app (confirming DI resolved the new dependency cleanly): USD wallet `1,000 → 0`, SAR wallet `0 → 3,750`, stored `ExchangeRate=3.750000`/`ConvertedAmount=3750.000` — byte-for-byte the spec's own example. Test data cleaned up; no `Program.cs` changes.
  - `dotnet build Meezan.sln` clean, 0 errors; `dotnet test Meezan.Tests` — 48/48 passing (41 prior + 6 resolver unit tests + 1 end-to-end integration test).

- Sub-task 7 (2026-08-09): `Meezan.Tests/RateIntegration/` — 15 tests covering all four named behaviors (§7.6), against the **real, unmodified** Phase 010 rate-integration classes. Zero production code changes this sub-task — every target class (`FrankfurterRateProvider`, `GoldApiRateProvider`, `RateResiliencePipeline`, `CompositeRateProvider`, `RateService`) was already constructor-injectable, so no extraction was needed. New shared test doubles in `TestSupport/`: `FakeHttpMessageHandler` (a scripted `HttpMessageHandler` — queued responses for exact sequences like "fail twice then succeed," plus an `AlwaysRespondJson` fallback for endpoints called repeatedly), `FakeRateProvider` (configurable `IRateProvider` for testing `RateService.SyncAsync`'s own persistence logic in isolation from HTTP), `FakeDistributedCache` (minimal in-process `IDistributedCache`, since `RateService`'s cache-aside read path isn't this sub-task's concern).
  - **Normalization** (`RateNormalizationTests.cs`, 5 tests): both providers' `GetMetalRateAsync` correctly divides the troy-ounce price by `31.1034768` for `RatePerGram`, and translates `XAU→GOLD`/`XAG→SILVER` at the provider boundary (nothing downstream ever sees the raw metal codes); `GetFiatRatesAsync` quotes carry a `RatePerUnit` only, `RatePerGram=null`; confirmed `GoldApiRateProvider.GetFiatRatesAsync` throws `NotSupportedException` (metals-only fallback, no FX endpoint).
  - **Circuit-breaker-open** (`RateResiliencePipelineTests.cs`, 4 tests): stays closed on success; opens once failures exceed the configured ratio/throughput; **fails fast without invoking the wrapped action at all** once open (proven via an invocation counter that stays `0`); stays closed when failures remain a small minority of calls. Had to work around Polly's `MaxRetryAttempts` validation (must be ≥1, so `0` — my first attempt, meant to isolate the breaker from retry noise — is rejected outright) by using `1` everywhere and sizing each test's failure/success counts with enough margin that the exact retry-attempt-vs-circuit-breaker-sample accounting doesn't matter either way.
  - **Fallback path** (`CompositeRateProviderFallbackTests.cs`, 3 tests): a failing Frankfurter metal quote (after its 1 configured retry, both failing) falls back to gold-api.com, confirmed via each fake handler's own request count (`2` then `1`); Frankfurter succeeding never touches GoldApi at all; a failing **fiat** quote correctly propagates with no fallback (gold-api.com has no FX endpoint — matches `CompositeRateProvider`'s own comment).
  - **Append-only** (`RateServiceSyncAppendOnlyTests.cs`, 3 tests, SQLite in-memory fixture — same reasoning as sub-tasks 4-6, `SyncAsync` runs inside `ExecuteInTransactionAsync`): a second `SyncAsync` run inserts a brand-new `RateSnapshot` row rather than updating the first (`1 → 2` rows, not `1 → 1` mutated); metals are stored at `RatePerGram`, not the raw per-troy-ounce figure; 5 consecutive runs leave 5 distinct historical rows per pair, matching `GetLatestByPairAsync`'s "most recent `FetchedAt` wins" resolution having real history to select from.
  - `dotnet build Meezan.sln` clean, 0 errors; `dotnet test Meezan.Tests` — 63/63 passing (48 prior + 15 new).

- Sub-task 8 (2026-08-10): rewrote `.github/meezan-spec.md` (185 insertions, 71 deletions) to fold in every correction accumulated over Phases 005–014, so the canonical spec — the file BOTH this repo and the future frontend repo defer to — stops disagreeing with what's actually shipped. Documentation-only; no code changes, confirmed by a clean `dotnet build`/`dotnet test` (63/63) afterward.
  - **BR-15/BR-17 time-driven-cycle revision** (Live Spec Correction): rewrote both rules — cycles are time-driven, not payment-driven; a new Active cycle starts unconditionally the same Hijri day a cycle goes Due, regardless of payment; an account can hold multiple simultaneous Due cycles. Redrew §4.3's hawl state-machine diagram to match (previously showed a single linear Active→Due→Paid→new-Active chain gated on payment).
  - **Gold-gram-anchored Zakat debt** (Round-2/BR-13/14/17 refinement): added a new §4.5 "Payment model" spelling out `ZakatDueGoldGrams`/`zakatGoldGrams`/`externalPaidGoldGrams`, the "stored once, never revalued" rule, and the read-time-only currency display guarantee. Updated the ZAKAT_CYCLES and TRANSACTIONS ERD blocks (`zakatAmountDue`→`zakatDueGoldGrams`, added `externalPaidGoldGrams`/`zakatCycleId`/`zakatGoldGrams`, removed the one-to-one `payTransactionId` in favor of the reverse FK) and the ERD relationship cardinality (`|o--||` → `|o--o{`, one cycle to many linked expenses).
  - **The protected Zakat/Charity category**: added as a BR-06 exception, a CATEGORIES.`isProtected` ERD field, a new §5.2 modeling-decision bullet, and folded into UC-01 (account creation) and UC-24 (Pay Zakat always posts to it).
  - **Currency-list growth via Frankfurter** (BR-01/02 refinement): rewrote both rules to state the `Currencies` table is not a fixed enum and grows at runtime via the rate-sync job; updated §2's Wallet bullet, the CURRENCIES ERD comment, and API row 4's description to match.
  - **Rate resolution algorithm** (§7): added the direct/inverse/cross-through-USD resolution steps (with the exact `EGP→SAR`/`SAR→GOLD` worked examples from the plan) that `RateService.GetLatestAsync` actually implements, plus corrected the architecture diagram (USD-pivot fiat call, currency-sync call, "append-only, never updates" note) and the RATE_SNAPSHOTS ERD comment (XAU/XAG never reach the DB).
  - **API contract**: grew from 28 to 29 rows (new `POST /api/zakat/cycles/{id}/external-payment`), and updated rows 17/25/26/27 to reflect `zakatCycleId` exposure on transactions, multi-cycle status, and the grams-based Pay Zakat contract.
  - **Use cases**: rewrote UC-21/22/24 (multi-cycle display, the cascading-due-boundaries case, partial/external payment, deletion revert) to match; UC-01 now mentions the protected category.

## Approval Log

| Sub-Task | Approved By | Date       |
| -------- | ----------- | ---------- |
| Plan     | User        | 2026-08-07 |
