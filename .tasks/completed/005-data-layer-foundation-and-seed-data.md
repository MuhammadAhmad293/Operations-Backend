# 005 – Data Layer Foundation & Seed Data

## Status

`completed`

## Goal

Create every entity in spec §5 ERD (Account, Currency, WalletType, Wallet, Category,
Transaction, Attachment, RateSnapshot, ZakatCycle), their EF configurations, repositories,
UnitOfWork wiring, bootstrap seed data, and the migration. Nothing else in the Meezan backend
can start before this lands.

## Context

First phase of the full Meezan backend implementation plan approved on 2026-08-07. Full
technical detail (exact field lists, precision decisions, FK delete-behavior, the Live Spec
Correction on Zakat cycles, and the Round-2 gold-gram refinement) lives in the approved plan
at `C:\Users\lenovo\.claude\plans\we-are-starting-a-lively-pudding.md` — this task file tracks
execution against that plan one sub-task at a time, per `.results/TASK_PLANNING_AND_WORKFLOW.md`.

Covers: BR-01…09, BR-13, BR-19 (schema only); spec §5, §5.2; `meezan-backend.md` §2.

## Sub-Tasks

| #   | Description                                                                                                                                       | Status     |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 0   | Housekeeping (unrelated to Meezan): move `.tasks/active/004-refresh-token-flow.md` to `.tasks/completed/`, update its `Status` field                | ✅ done |
| 1   | Enums: `TransactionType`, `CategoryKind`, `CurrencyType`, `DisplayCalendar`, `Theme`, `Language`, `ZakatCycleStatus` in `Meezan.DataModel/Enums/`    | ✅ done |
| 2   | Lookup entities + configs: `Currency` (PK `Code` string), `WalletType` (int PK)                                                                    | ✅ done |
| 3   | `Account` entity + config                                                                                                                          | ✅ done |
| 4   | `Wallet` entity + config                                                                                                                           | ✅ done |
| 5   | `Category` entity + config (incl. `IsProtected` bool)                                                                                              | ✅ done |
| 6   | `Transaction` entity + config (incl. `ZakatCycleId` FK, `ZakatGoldGrams`)                                                                          | ✅ done |
| 7   | `Attachment` entity + config                                                                                                                       | ✅ done |
| 8   | `RateSnapshot` entity + config                                                                                                                     | ✅ done |
| 9   | `ZakatCycle` entity + config (`PotGoldGramsAtDue`, `ZakatDueGoldGrams`, `ExternalPaidGoldGrams` — gram-denominated per Round-2 refinement)          | ✅ done |
| 10  | `DbSet<T>` × 9 on `AppDbContext`; repositories + `IUnitOfWork`/`UnitOfWork` wiring × 9                                                             | ✅ done |
| 11  | Seed data: `Currency` (USD, SAR, EGP, GOLD, SILVER), `WalletType` (General, Bank Account, Cash, …) — no `Category` seed (account-scoped, see Phase 006) | ✅ done |
| 12  | Migration `AddMeezanCoreEntities`, apply to local DB, confirm `dotnet build` clean across all projects                                            | ✅ done |

## Notes

- Sub-task 0 (2026-08-07): moved `004-refresh-token-flow.md` from `.tasks/active/` to `.tasks/completed/` and updated its `Status` field to `completed` (all 9 sub-tasks were already ✅ done and the feature is already merged to `master`, commit `836f81f` — this was pure housekeeping, unrelated to Meezan).
- Sub-task 1 (2026-08-08): created `TransactionType`, `CategoryKind`, `CurrencyType`, `DisplayCalendar`, `Theme`, `Language`, `ZakatCycleStatus` in `Meezan.DataModel/Enums/` (matching the existing `RefreshTokenRevocationReason`/`OutboxStatus` pattern — `public enum` with explicit int values, `namespace Meezan.DataModel.Enums`). `Meezan.DataModel` builds with 0 errors (26 pre-existing warnings, unrelated).
- Sub-task 2 (2026-08-08): created `Currency` (extends `BaseMultilingualTextEntity`, PK `Code` string, `Type` (`CurrencyType`), `Symbol`, `Decimals`) and `WalletType` (extends `BaseMultilingualTextEntity`, int PK `WalletTypeId` via EF's `{TypeName}Id` convention, same as `MailStatusId`/`MailTypeId`) entities, plus their `internal IEntityTypeConfiguration<T>` classes — `CurrencyConfiguration` explicitly sets `HasKey(e => e.Code)` (natural-key PK, not the usual `int` convention) and stores `Type` via `HasConversion<string>()` (first use of this pattern in the repo, per the plan). `Meezan.Repositories` builds with 0 errors (56 pre-existing warnings, unrelated).
- Sub-task 3 (2026-08-08): created `Account` (extends `BaseEntity`; `UserId`, `Name`, `BaseCurrencyCode`, `DisplayCalendar`/`Theme`/`Language`, nav to `User` and `Currency`) and `AccountConfiguration` (unique index on `UserId` — one account per user, BR-01; both `UserId→User` and `BaseCurrencyCode→Currency` FKs set `DeleteBehavior.Restrict` per the plan's FK decisions; all three enums stored via `HasConversion<string>()`). `Meezan.Repositories` builds with 0 errors (35 pre-existing warnings, unrelated).
- Sub-task 4 (2026-08-08): created `Wallet` (extends `BaseEntity`; `AccountId`, `WalletTypeId`, `CurrencyCode`, `InitialAmount decimal(18,3)` defaulted 0, nullable `Color`/`Icon`, `ExcludeFromTotal`, `IsArchived`) and `WalletConfiguration` (index on `AccountId`; `Account` FK `Cascade` — a wallet is an owned child of its account, not previously covered by the plan's explicit FK list, decided consistently with the rest of the aggregate; `WalletType`/`Currency` FKs `Restrict`, matching the plan's lookup-protection rule). `Meezan.Repositories` builds with 0 errors (40 pre-existing warnings, unrelated).
- Sub-task 5 (2026-08-08): created `Category` (extends `BaseEntity`; `AccountId`, self-ref `ParentId`/`Parent`/`Children`, `Kind`, `Name`, nullable `Color`/`Icon`, `SortOrder` default 0, `IsProtected` bool default `false` — blocks user deletion/edit of the system Zakat/Charity category created per-account in Phase 006) and `CategoryConfiguration` (`Account` FK `Cascade`; self-ref `Parent`→`Children` FK `Restrict` — depth/soft-delete rules are enforced in the service layer per BR-06, not via a DB cascade). `Meezan.Repositories` builds with 0 errors (43 pre-existing warnings, unrelated).
- Sub-task 6 (2026-08-08): created `Transaction` (extends `BaseEntity`; all ERD fields — `Type`, dual date as `DateOnly DateGregorian`/`string DateHijri` (max length 10, sortable `yyyy-MM-dd` Hijri format per the plan)/`TimeOnly Time`, `Amount decimal(18,3)`, `WalletId`/`ToWalletId`, `CategoryId`, `Karat`, `PureGoldGrams decimal(18,3)`, `ExchangeRate decimal(18,6)`, `ConvertedAmount decimal(18,3)`, `IsFee`, self-ref `ParentTransactionId`/`ParentTransaction`/`FeeTransactions`, `Description`/`Note`, plus `ZakatCycleId`/`ZakatGoldGrams decimal(18,3)` per the Round-2 refinement) and `TransactionConfiguration` (`Account` FK `Cascade`; `Wallet`/`ToWallet`/`Category` FKs `Restrict` — history must survive per BR-06; self-ref fee FK `Cascade`, literal BR-09; composite index `(AccountId, DateGregorian)` for filtered-list queries, plus an index on `ZakatCycleId`). **Sequencing note**: `ZakatCycleId` is added now as a plain `int?` column with no FK constraint yet — `ZakatCycle` doesn't exist until sub-task 9, so wiring the actual `HasOne/WithMany.OnDelete(Restrict)` relationship (and the `Attachment.TransactionId` cascade) is deferred to when those entities land, to avoid a forward reference to a not-yet-created type. `Meezan.Repositories` builds with 0 errors (47 pre-existing warnings, unrelated — new `CS8618` warnings on `Transaction`'s non-nullable navs match the existing pattern already seen on `RefreshToken`/`Mail`).
- Sub-task 7 (2026-08-08): created `Attachment` (extends `BaseEntity`; `TransactionId`, `FileName`, `MimeType`, `SizeBytes`, `StoragePath`) and `AttachmentConfiguration` (`Transaction` FK `Cascade` — the DB row cascade is now in place; the plan's physical-file-cleanup requirement, since a DB cascade can't touch disk, is a Phase 009 service-layer concern). `Meezan.Repositories` builds with 0 errors (51 pre-existing warnings, unrelated).
- Sub-task 8 (2026-08-08): created `RateSnapshot` (extends `BaseEntity`; `FromCurrencyCode`/`ToCurrencyCode` FKs — renamed from the ERD's bare `fromCurrency`/`toCurrency` to add the `Code` suffix, matching the `Wallet.CurrencyCode`/`Account.BaseCurrencyCode` naming convention already established, with nav properties `FromCurrency`/`ToCurrency`; `Rate decimal(18,6)`, `FetchedAt`, `Source`) and `RateSnapshotConfiguration` (composite index `(FromCurrencyCode, ToCurrencyCode, FetchedAt)` for the Phase 010 latest-snapshot lookup; both FKs `Restrict` per the plan — rows are append-only and never mutated or deleted). `Meezan.Repositories` builds with 0 errors (56 pre-existing warnings, unrelated).
- Sub-task 9 (2026-08-08): created `ZakatCycle` (extends `BaseEntity`; `AccountId`, `HawlStartHijri`/`HawlDueHijri` (sortable Hijri strings), `Status` enum-as-string, nullable `PotGoldGramsAtDue`/`ZakatDueGoldGrams`/`ExternalPaidGoldGrams` all `decimal(18,3)` — gram-denominated per the Round-2 refinement, no fiat `ZakatAmountDue`/`DueCurrency`, no `PayTransactionId`) and `ZakatCycleConfiguration` (`Account` FK `Cascade`; composite index `(AccountId, Status)` for the Phase 012 active/due/history queries). **Completes the sub-task-6 deferral**: wired the `Transaction.ZakatCycleId` FK here via `HasMany(e => e.Payments).WithOne().HasForeignKey(t => t.ZakatCycleId).OnDelete(Restrict)` — no nav needed on `Transaction`'s side, matching the `Payments` collection nav on `ZakatCycle`. `Meezan.Repositories` builds with 0 errors (60 pre-existing warnings, unrelated).
- Sub-task 10 (2026-08-08): added `DbSet<T>` for all 9 new entities to `AppDbContext`; created `I{Entity}Repository`/`{Entity}Repository` pairs for all 9 (generic CRUD only, matching the minimal `IUserRepository`/`UserRepository` shape — no entity-specific query methods yet, those land lazily in later phases); wired all 9 into `IUnitOfWork` and `UnitOfWork`. Built the full solution (`dotnet build Meezan.sln`) to confirm the `IUnitOfWork` interface change didn't break its one implementer or any consumer — 0 errors across every project (71 pre-existing warnings, unrelated).
- Sub-task 11 (2026-08-08): added `#region Currency`/`#region WalletType` blocks to `InitialDataSeeding.SeedInitialData` (same `HasData(...)` + fixed static `DateTime` literal pattern as the existing `MailStatus`/`MailType` seeds). `Currency`: USD/SAR/EGP (`CurrencyType.Fiat`, 2 decimals), GOLD/SILVER (`CurrencyType.Metal`, 3 decimals) — EN/AR names, symbols, descriptions. `WalletType`: General/Bank Account/Cash (`WalletTypeId` 1–3, literal ints — no existing `Common.Enums` entry to cast from, unlike `MailStatusEnum`). No `Category` seed, per sub-task 5's note (account-scoped, not a shared lookup). `Meezan.Repositories` builds with 0 errors (3 pre-existing warnings, unrelated).
- Sub-task 12 (2026-08-08): ran `dotnet ef migrations add AddMeezanCoreEntities` — generated cleanly on the first attempt (correct FKs, precisions, indexes, seed `InsertData`). **`dotnet ef database update` failed** on the real SQL Server instance: `Transaction.ParentTransactionId`'s `ON DELETE CASCADE` (BR-09) combined with `Transaction.AccountId`'s `ON DELETE CASCADE` (from `Account`) forms a "multiple cascade paths" cycle SQL Server refuses to create (error 1785) — a constraint the ERD/build alone couldn't surface. Confirmed via `sys.tables` that the failed attempt rolled back fully (0 partial tables), fixed `TransactionConfiguration` to set `ParentTransaction` FK to `Restrict` (BR-09's cascade-delete-the-fee guarantee moves to `TransactionService.Delete` in Phase 009, not the DB), removed and regenerated the migration, and applied it successfully. Verified via `sqlcmd`: all 9 tables exist, `Currency` has the 5 seeded rows, `WalletType` has the 3 seeded rows. Full solution (`dotnet build Meezan.sln`) builds with 0 errors (43 pre-existing warnings, unrelated).
- **Phase 005 complete.** All 9 Meezan entities, configs, repositories, UnitOfWork wiring, bootstrap seed data, and the migration are in place. Data model deviations from the original plan discovered during execution (both now reflected in the live schema, plan updates recommended for the Phase 014 spec-sync sub-task): (1) `RateSnapshot.FromCurrency`/`ToCurrency` renamed to `FromCurrencyCode`/`ToCurrencyCode` for naming consistency; (2) `Transaction.ParentTransactionId→Transaction` is `Restrict` at the DB level, not `Cascade` — Phase 009 must explicitly delete the linked fee transaction in `TransactionService.Delete` rather than relying on a DB cascade.

## Approval Log

| Sub-Task | Approved By | Date       |
| -------- | ----------- | ---------- |
| Plan     | User        | 2026-08-07 |
