# 009 – Transactions, Fees & Attachments

## Status

`completed`

## Goal

Implement API contract rows #14–19: the core write path (income/expense/transfer), linked
fees, dual calendar, cross-currency conversion, and file attachments.

## Context

Fifth phase of the full Meezan backend implementation plan approved on 2026-08-07. Depends on
Phase 005–008. Every later read-side view (overview, calendar, statistics, zakat) depends on
this being correct. Full technical detail lives in the approved plan at
`C:\Users\lenovo\.claude\plans\we-are-starting-a-lively-pudding.md`.

Covers: BR-06, BR-07, BR-08, BR-09, BR-10; UC-12…16; API #14–19; spec §10 (attachments).

## Sub-Tasks

| #   | Description                                                                                                                        | Status     |
| --- | ------------------------------------------------------------------------------------------------------------------------------------ | ---------- |
| 1   | `Common/HijriCalendar` helper (sortable `yyyy-MM-dd` Hijri format fixed here for all downstream phases)                            | ✅ done |
| 2   | `TransactionDto` (incl. `ZakatCycleId`), `CreateTransactionDto`, `UpdateTransactionDto` + `TransactionMapper`                       | ✅ done |
| 3   | `ITransactionRepository`: `GetFilteredAsync`, `SearchAsync`                                                                         | ✅ done |
| 4   | `ITransactionService`/`TransactionService.Add`: BR-07 validation, karat math, cross-currency rate, atomic fee creation, Zakat hook | ✅ done |
| 5   | `TransactionService.Update`/`Delete`: BR-06 dropdown semantics, fee cascade, Zakat payment recompute hook, physical file cleanup    | ✅ done |
| 6   | Attachments: `IFileStorageService` (local disk), `AttachmentDto`, `IAttachmentService`/`AttachmentService`                          | ✅ done |
| 7   | `TransactionController`, `AttachmentController` (first `IFormFile` code in the repo)                                                | ✅ done |
| 8   | Localization keys incl. `ZakatPaymentDeletionWarning`                                                                               | ✅ done |
| 9   | Settings: `FileStorageSettings`, DI registration                                                                                    | ✅ done |
| 10  | Manual verification: fee-atomicity, karat math, cross-currency math, dropdown behavior, attachment upload/download/disk-cleanup    | ✅ done |

## Notes

- Sub-task 1 (2026-08-08): created `IHijriCalendarHelper`/`HijriCalendarHelper` (`Common/HijriCalendar/`, wrapping `System.Globalization.UmAlQuraCalendar`), registered in `CommonResolver` (matches the documented `common-utilities` convention: interface + implementation pair). Surface: `ToHijriString(DateOnly)`, `ToGregorian(string)`, `AddHijriYears(string, int)`, `AddDays(string, int)` — the last two cover the "+1 Hijri year" (hawl due date) and "−14 days" (login reminder trigger) arithmetic Phases 012/013 need. Storage format fixed as zero-padded sortable `yyyy-MM-dd` Hijri so callers compare dates as plain strings, never parsing. **Verified against real calendar math, not just compiled**: wrote a standalone scratch console script exercising the actual `UmAlQuraCalendar` API and confirmed two independently well-known reference conversions match exactly (2000-01-01 Gregorian → 1420-09-24 Hijri; 1979-11-20 Gregorian → 1400-01-01 Hijri, the widely-documented start of the 15th Hijri century), plus a round-trip (Hijri→Gregorian→Hijri returns the identical date), `+1 Hijri year` and `−14 days` arithmetic, and that the sortable string format's lexicographic ordering matches true chronological ordering. Deleted the scratch project afterward. `dotnet build Meezan.sln` succeeds with 0 errors (71 pre-existing warnings, unrelated).
- Sub-task 2 (2026-08-08): created `TransactionDto` (all ERD fields incl. `ZakatCycleId`), `TransactionGroupDto` (`{ Date, List<TransactionDto> }` — the BR-11 grouped-by-day list shape), `CreateFeeDto` (`{ Amount }`, nested on `CreateTransactionDto.Fee`), `CreateTransactionDto`, `UpdateTransactionDto` (`Meezan.Dto/DTOs/Transaction/`), plus `TransactionMapper`. Deliberately excluded from `CreateTransactionDto`/`UpdateTransactionDto`: `DateHijri` (always server-computed via `IHijriCalendarHelper` from `DateGregorian`, BR-08) and `PureGoldGrams` (always server-computed from `Amount`×`Karat`÷24, BR-03 explicitly forbids trusting a client-supplied pure value). `dotnet build Meezan.sln` succeeds with 0 errors (131 pre-existing warnings, unrelated).
- Sub-task 3 (2026-08-08): added `TransactionFilter` (`Meezan.IRepositories/IRepository/`, query-layer filter — not client-facing) and `ITransactionRepository.GetFilteredAsync(accountId, filter)` (composable optional `From`/`To`/`WalletId`(matches source or destination)/`CategoryId`/`Type` predicates, newest-first)/`SearchAsync(accountId, query)` (`EF.Functions.Like` across `Description`/`Note`/`Category.Name`/`Wallet.Name`, UC-18). **Verification note**: these are non-trivial EF Core translations (a nullable-navigation-property `Like` in particular); no controller exists yet to exercise them live, so full verification is explicitly deferred to sub-task 10 rather than skipped — flagged here so it isn't forgotten. `dotnet build Meezan.sln` succeeds with 0 errors (131 pre-existing warnings, unrelated).
- Sub-task 4 (2026-08-08): the largest/most complex logic in the phase so far. Created `IZakatEngine`/`ZakatEngine` placeholder (no-op, same forward-dependency pattern as `IRateService` from Phase 006 — Phase 012 replaces this file entirely), 4 new localization keys (`WalletsMustDiffer`, `TransactionSaved`, `TransactionNotFound`, `CategoryKindMismatch`), and `ITransactionService`/`TransactionService` (interface defined partially — `GetFiltered`/`Search`/`GetById`/`Add` now, `Update`/`Delete` extend it in sub-task 5, same incremental-interface pattern used for the repository interfaces throughout this session). `Add`: BR-07 type-conditional validation (Transfer requires differing wallets — `WalletsMustDiffer`; Income/Expense requires a category whose `Kind` matches the transaction `Type` — new `CategoryKindMismatch` guard, not explicitly in the original plan but needed for Statistics/Zakat correctness later), BR-03 karat computation (GOLD-currency-code only, validated against the 5 allowed karats, `PureGoldGrams` always server-computed — client value never trusted), BR-08 server-side Hijri conversion via Phase 009 sub-task 1's helper, BR-10 cross-currency resolution (explicit `ConvertedAmount` override derives the audit `ExchangeRate`; explicit `ExchangeRate` override derives `ConvertedAmount`; neither supplied falls back to `IRateService.GetLatestAsync`), fee creation wired via the `ParentTransaction` navigation property (so EF fixes up the FK once `Id` is assigned by `SaveChanges`, inheriting the parent's karat) atomically via `ExecuteInTransactionAsync`, with the Zakat re-evaluation hook called **inside** that same transactional delegate so Phase 012's real writes will share the identical commit. `GetFiltered`/`Search`/`GetById` are thinner: BR-11 period-to-date-range resolution (`daily`/`weekly`/`monthly`/`quarterly`/`yearly`/`all`, Sunday-start weeks) and the grouped-by-day response shape. **One real bug caught by the build**: a `static` helper (`ComputeKarat`) referenced the instance-level `Localization` property (CS0120) — fixed by making it an instance method, same class of bug as `GroupByDay`'s Mapster inconsistency (also fixed: was using the static `Adapt<T>()` extension instead of the injected `Mapper.Map<T>()` every other service in this codebase uses). **Verification note**: `TransactionController` doesn't exist until sub-task 7, so live HTTP verification of `Add`'s karat/fee/cross-currency logic is deferred to sub-task 10, same reasoning as sub-task 3. `dotnet build Meezan.sln` succeeds with 0 errors (160 pre-existing warnings, unrelated).
- Sub-task 5 (2026-08-08): pulled forward several pieces genuinely required by this sub-task's scope (not deferred, since Update/Delete's correctness depends on them per the plan):
  - **`Common/FileStorage/IFileStorageService`/`FileStorageService`** (full local-disk implementation — save under a year/month subfolder with a GUID-prefixed filename, delete, open-read; Decision D2) + `FileStorageSettings` (`UploadRootPath`), bound in `Program.cs` the same way as `JwtSettings`/`RefreshTokenSettings`, registered in `CommonResolver`. This is sub-task 6/9 material pulled forward because BR-09's physical-file-cleanup requirement genuinely needs it now.
  - **`IZakatEngine.RecomputeCyclePaymentAsync(zakatCycleId, ct)`** — second method (Phase 012 material), no-op stub alongside `ReevaluateAsync`.
  - **`ITransactionRepository.GetFeesByParentIdAsync`** and **`IAttachmentRepository.GetByTransactionIdsAsync`** — proper scoped repository queries (not the "fetch everything, filter in memory" anti-pattern I've deliberately avoided elsewhere this session).
  - **`TransactionDto`** gained `WalletName`/`ToWalletName`/`CategoryName` — BR-06 requires a soft-deleted wallet/category to still "appear as the currently selected value," but the API contract has no single-wallet/category-by-id endpoint, so the frontend has no other way to resolve a soft-deleted reference's display name without this being denormalized onto the transaction itself. Populated only by `GetById` (the UC-15 "open transaction" edit flow) — deliberately left null on the list/search views for query-cost reasons, since BR-06's requirement is specifically about the edit flow.
  - Extracted a shared `ValidateAndResolveAsync` private helper (BR-07/BR-03/BR-08/BR-10 logic) reused by both `Add` and `Update` — avoided ~50 lines of duplication; returns a private nested `ResolvedTransaction` record.
  - `Update`: re-validates against the new values (wallet/category lookups scoped to `!IsDeleted`, which is exactly what enforces BR-06's "once changed away from a soft-deleted value, it can never be re-selected"), calls both Zakat hooks inside the same `ExecuteInTransactionAsync` delegate.
  - `Delete`: finds linked fee(s) via `GetFeesByParentIdAsync` and deletes them explicitly (BR-09 — the DB FK is `Restrict`, not `Cascade`, per Phase 005 sub-task 12's discovery), collects attachment `StoragePath`s for the parent + fees *before* the DB transaction, deletes the DB rows atomically (with both Zakat hooks), then best-effort deletes the physical files afterward — a failure there is logged via newly-injected `ILogger<TransactionService>`, never allowed to fail an already-committed delete.
  - **Verified `FileStorageService` in isolation** (referencing the real `Common.csproj`, not a reimplementation): save→read-back→delete round-trip matches exactly, relative paths use forward slashes, and deleting an already-deleted/nonexistent file doesn't throw (idempotent, as `Delete`'s best-effort cleanup requires). Deleted the scratch project afterward.
  - **Verification note carried forward**: full end-to-end HTTP verification of Update/Delete (BR-06 dropdown behavior, fee cascade, file cleanup) still needs `TransactionController` (sub-task 7) — deferred to sub-task 10 as planned.
  - `dotnet build Meezan.sln` succeeds with 0 errors (131 pre-existing warnings, unrelated).
- Sub-task 6 (2026-08-08): `IFileStorageService`/`FileStorageService` were already fully built and isolation-verified in sub-task 5's pull-forward — nothing new needed there. Created `AttachmentDto`, `AttachmentContentDto` (`Meezan.Dto/DTOs/Attachment/`, the latter not persisted — it's `Download`'s response payload: `Stream`/`FileName`/`MimeType`), `AttachmentMapper`, and 3 localization keys (`AttachmentNotFound` new, `InvalidAttachmentType`/`AttachmentTooLarge` pulled forward from sub-task 8). `IAttachmentService`/`AttachmentService`: `Upload` (validates size ≤10MB and extension+MIME-type together against an allow-list of `.pdf`/`.jpg`/`.jpeg`/`.png`/`.gif`/`.webp` before ever touching disk, per spec §10; if the DB commit fails after `FileStorageService.SaveAsync` already wrote the file, the file is deleted immediately so a failed upload never leaves an orphan), `Download`/`Delete` (both scoped via `a.Transaction.AccountId == account.Id` — a navigation-property join expressed directly in the generic repository's predicate, the first use of that specific pattern in this codebase, though the same general EF Core capability as sub-task 3's `SearchAsync`; `Delete` reuses `TransactionService.Delete`'s best-effort-log-don't-fail physical-cleanup shape). Deliberately kept `IFormFile` out of `IAttachmentService`'s signature (`Upload` takes a plain `Stream`/`fileName`/`mimeType`/`sizeBytes`) so `Meezan.IServices` stays free of ASP.NET Core-specific types — the controller (sub-task 7) will do that extraction. **Verification note**: like sub-task 3's `SearchAsync`, the new nav-property-join predicate is deferred to sub-task 10's live verification since `AttachmentController` doesn't exist yet. `dotnet build Meezan.sln` succeeds with 0 errors (136 pre-existing warnings, unrelated).
- Sub-task 7 (2026-08-08): created `TransactionController` (`GET /api/transactions`, `GET /api/transactions/search`, `GET/POST/PUT/DELETE /api/transactions[/{id}]`) and `AttachmentController` (`POST /api/transactions/{id}/attachments` via an absolute `[HttpPost("/api/transactions/{transactionId:int}/attachments")]` route override — the class's own base route is `api/attachments`; `GET/DELETE /api/attachments/{id}`) — the first `IFormFile` code in the repo. `Download` is the first controller action here that can't follow the thin `Ok(ResponseDto)` convention — it unwraps `response.Data` and returns a `File()` result instead, a necessary deviation since streaming binary content needs a different `IActionResult` shape; `Upload` does a minimal `file is null` guard directly in the controller (throwing `InvalidRequestException()` with its default message) since `IAttachmentService.Upload` deliberately takes a plain `Stream` rather than `IFormFile`, so no service call is possible to validate this first. **Minimal smoke test** (not the full sub-task 10 matrix): started the API, confirmed the whole DI chain resolves cleanly (`TransactionService`, `AttachmentService`, `ZakatEngine`, `FileStorageService`, etc.), confirmed all 5 new routes appear in `swagger.json` with no conflicts, and called `GET /api/transactions`/`GET /api/transactions/search` successfully — which also resolves sub-task 3's deferred concern about `GetFilteredAsync`/`SearchAsync`'s EF Core query translation actually working at runtime. `dotnet build Meezan.sln` succeeds with 0 errors (128 pre-existing warnings, unrelated).
- Sub-task 8 (2026-08-08): added the one remaining key from the plan's list, `ZakatPaymentDeletionWarning` (confirmation text for deleting a `ZakatCycleId`-linked transaction — intentionally for frontend consumption, not called from backend service code). Audited all 8 keys added across this phase: each has exactly one `localizationFile.json` entry, one `ILocalizationService` property, one `LocalizationService` implementation, and all except `ZakatPaymentDeletionWarning` are actively used via `Localization.*` in `TransactionService`/`AttachmentService`. `dotnet build Meezan.sln` succeeds with 0 errors (100 pre-existing warnings, unrelated).
- Sub-task 9 (2026-08-08): `FileStorageSettings`/DI registration for `IFileStorageService`/`ITransactionService`/`IAttachmentService`/`IZakatEngine` were already fully done in sub-task 5's pull-forward — verified all present via grep. Added the one missing piece: an explicit `FileStorageSettings` section (`UploadRootPath: "Uploads"`) in `Meezan/appsettings.json`, matching every other bound settings object's convention of a visible, discoverable config section. `dotnet build Meezan.sln` succeeds with 0 errors (100 pre-existing warnings, unrelated).
- Sub-task 10 (2026-08-08): the most comprehensive live test matrix run in this session so far. Set up a USD wallet and a GOLD wallet alongside the existing SAR Cash wallet, then exercised:
  - **Fee atomicity**: `POST` Income+fee in one call → both rows created, balance reflects both (`1000+500-10=1490`); `DELETE` the parent → both parent and fee return 404 and are confirmed genuinely gone from the DB (not soft-deleted — transactions have no soft-delete concept), correctly bypassing the `Restrict` FK via the explicit `GetFeesByParentIdAsync` + delete logic from sub-task 5.
  - **Karat math**: `24g@22K→22.000`, `10g@24K(default)→10.000`, and after an `Update`, `15g@18K→11.250` — all exact; karat `19` correctly rejected (400); Gold wallet balance stays in raw grams (`34.000`), never the pure-equivalent, matching BR-03's wallet-screen display rule.
  - **Cross-currency (BR-10)**: `exchangeRate` override → correct `convertedAmount`; `convertedAmount` override (`26.50` on a `100` transfer) → correctly derived audit rate `0.265000`; no override → correctly falls through to `RateService.GetLatestAsync`, which correctly 503s (Phase 006's placeholder, Phase 010 not built yet) rather than silently returning a wrong rate.
  - **Validation guards**: same-wallet transfer (`WalletsMustDiffer`), Income tagged with an Expense category (`CategoryKindMismatch`), Expense with no category, negative amount — all 400 as designed.
  - **BR-06 dropdown behavior**: soft-deleted a wallet referenced by an existing transfer → confirmed absent from `GET /api/wallets` (DB: `IsDeleted=1`, not hard-deleted) but `GetById` on the referencing transaction still resolves `toWalletName: "USD Wallet"` correctly — the exact "selected but not in dropdown" behavior BR-06 requires.
  - **Attachments**: upload (correct MIME-type/size auto-detection), disallowed type (`.exe`) → 400 `InvalidAttachmentType`, download returned byte-for-byte identical content, direct `DELETE /api/attachments/{id}` removed the physical file (confirmed via filesystem check), and — the trickiest case — **deleting the parent transaction removed both the DB-cascaded `Attachment` row and the physical file on disk**, confirming the sub-task 5 cleanup design works end-to-end, not just in isolation.
  - **Cross-account scoping**: a second registered user got 404 on the first user's transaction, never leaking existence.
  - **BR-11 period filters** (`daily`, `monthly`) and **search** (`q=Salary`, matched via the `Category.Name` join) both returned correct results.
  - Curl's `-F "file=@path;type=..."` syntax failed to parse in this Git Bash environment (a shell/curl quirk, not an app bug) — worked around by omitting the explicit type override and letting curl auto-detect from the file extension, which it did correctly.
  - Cleaned up all test transactions/wallets/uploaded files and stopped the app afterward.
  - `dotnet build Meezan.sln` — 0 errors throughout, no code changes needed in this sub-task (verification only).
- **Phase 009 complete.** All 6 API contract rows (#14–19) implemented and thoroughly live-verified — the largest phase in the plan.

## Approval Log

| Sub-Task | Approved By | Date       |
| -------- | ----------- | ---------- |
| Plan     | User        | 2026-08-07 |
