# 010 – Rate Integration Architecture

## Status

`completed`

## Goal

Implement the full rate-integration architecture from spec §7: adapters, composite fallback,
resilience, scheduled + startup sync job, Redis cache, append-only snapshots, and API contract
row #24.

## Context

Sixth phase of the full Meezan backend implementation plan approved on 2026-08-07. Backfills
the `IRateService.GetLatestAsync` interface stubbed in Phases 006/009. Full technical detail
(incl. the USD-pivot fetch strategy and the direct/inverse/cross-through-USD resolution
algorithm) lives in the approved plan at
`C:\Users\lenovo\.claude\plans\we-are-starting-a-lively-pudding.md`.

Covers: BR-19; spec §7; `meezan-backend.md` §4; API #24.

## Sub-Tasks

| #   | Description                                                                                                       | Status     |
| --- | --------------------------------------------------------------------------------------------------------------- | ---------- |
| 1   | `RateIntegrationSettings` + `appsettings.json` section                                                          | ✅ done |
| 2   | `IRateProvider` + `FrankfurterRateProvider` (USD-pivot only)                                                    | ✅ done |
| 3   | `GoldApiRateProvider` — spike the live contract first                                                           | ✅ done |
| 4   | `RateResiliencePipeline` (Polly retry + circuit breaker)                                                        | ✅ done |
| 5   | `CompositeRateProvider` (Frankfurter first, GoldApi fallback for metals)                                        | ✅ done |
| 6   | Redis: package, DI registration, `RedisHealthCheck`                                                             | ✅ done |
| 7   | `IRateService`/`RateService`: `SyncAsync` (+ currency sync from Frankfurter), `GetLatestAsync` (direct/inverse/cross-through-USD resolution) | ✅ done |
| 8   | `IJobService.SyncRates()` + recurring registration + startup fire-and-forget enqueue                            | ✅ done |
| 9   | `RatesController`                                                                                               | ✅ done |
| 10  | Localization: `RatesUnavailable`                                                                                | ✅ done |
| 11  | Manual verification: fallback, circuit-breaker, cross/inversion resolution, currency-sync, startup sync         | ✅ done |

## Notes

- Sub-task 1 (2026-08-08): created `RateIntegrationSettings` (`Meezan.Services/Setting/RateIntegrationSettings.cs`) with nested `FrankfurterSettings{BaseUrl}`/`GoldApiSettings{BaseUrl}`/`RedisSettings{ConnectionString}`, and `CronExpression` (default `"0 2 * * *"`, daily per BR-19's "daily is sufficient"). Reused the **existing** `ResilienceSettings` class from `EmailDeliverySettings.cs` directly for `.Resilience` rather than duplicating an identical shape — it's already a generic retry/circuit-breaker POCO, not email-specific. Added the `RateIntegration` section to `appsettings.json` (resilience `MinimumThroughput` tuned down to 5 vs. the email pipeline's 10, since a daily job has far lower call volume — a deliberate per-domain choice, not a copy-paste artifact) and bound it in `Program.cs` immediately after `EmailDeliverySettings`, same pattern. `dotnet build Meezan.sln` succeeds with 0 errors (43 pre-existing warnings, unrelated).
- Sub-task 2 (2026-08-08): created `RateQuote` (`Meezan.IServices/IService/RateQuote.cs` — `{FromCurrencyCode, ToCurrencyCode, RatePerUnit, RatePerGram?, FetchedAt, Source}`) and `IRateProvider` (`GetFiatRatesAsync(List<string> quoteCurrencyCodes)`, `GetMetalRateAsync(string metalCode)`, both throw rather than return null on failure so `CompositeRateProvider`'s sub-task-5 fallback can use plain exception handling). Implemented `FrankfurterRateProvider` (`Meezan.Services/RateProviders/FrankfurterRateProvider.cs`) as a typed `HttpClient` (`AddHttpClient<FrankfurterRateProvider>()` registered in `Program.cs` — first `IHttpClientFactory` use in this repo), USD-pivot only: `GetFiatRatesAsync` calls `GET /v2/rates?base=USD&quotes=<codes>`, `GetMetalRateAsync` calls `GET /v2/rates?base={XAU|XAG}&quotes=USD` and normalizes to per-gram via `÷31.1034768`, plus `XAU→GOLD`/`XAG→SILVER` code translation at this boundary so nothing downstream ever sees a raw provider code.
  - **Live-verification caught a real bug before it shipped**: my first implementation assumed Frankfurter v2's `/rates` endpoint returns a single JSON object `{base, date, rates: {code: value, ...}}` (a v1-style shape). Direct `curl` against the actual live API (`api.frankfurter.dev/v2/rates?base=USD&quotes=SAR,EGP`, plus the metals and `/v2/currencies` endpoints) showed the real response is a **JSON array**, one `{date, base, quote, rate}` object per quote currency — completely different shape, would have failed to deserialize (`GetFromJsonAsync` returning null → immediate `HttpRequestException` on every call). Rewrote the private response model as `FrankfurterRateEntry` (`List<FrankfurterRateEntry>`) with explicit `[JsonPropertyName]` attributes on every property (`date`/`base`/`quote`/`rate` are lowercase in the real payload; `System.Text.Json`'s default `GetFromJsonAsync` overload is case-**sensitive**, so relying on default PascalCase-matching would have silently bound everything to `null`/`0` instead of throwing — explicit attributes make the mapping correct regardless of serializer defaults). Also confirmed (for later use in sub-task 7's currency-sync) that `/v2/currencies` returns an array of `{iso_code, iso_numeric, name, symbol, start_date, end_date}` objects, not a code→name dictionary as originally assumed from the plan's description — will need the same array-based model when that sub-task is implemented.
  - Verified the fix with a scratch console app (`ProjectReference`s to the real `Meezan.Services`/`Meezan.IServices` projects, run from the scratchpad dir, deleted after) that instantiated the real `FrankfurterRateProvider` with a live `HttpClient` and called all three code paths against the actual API: `GetFiatRatesAsync(["SAR","EGP"])` → `USD→EGP=49.787`, `USD→SAR=3.75`; `GetMetalRateAsync("XAU")` → `GOLD→USD=4255.29` (gram=136.81...); `GetMetalRateAsync("XAG")` → `SILVER→USD=61.767` (gram=1.985...). All non-null/non-default, confirming both the array deserialization and the metal code translation work end-to-end. `dotnet build Meezan.sln` clean, 0 errors, 46 pre-existing warnings (unrelated).
- Sub-task 3 (2026-08-08): spiked gold-api.com's live contract via `curl` before writing any code (per the lesson from sub-task 2's Frankfurter surprise) — confirmed `GET /price/{symbol}` (e.g. `/price/XAU`) returns a single JSON object `{currency, currencySymbol, exchangeRate, name, price, symbol, updatedAt, updatedAtReadable}` with `price` = USD per troy ounce, and an unknown symbol returns HTTP 404 `{"error":"Symbol not found"}`. Implemented `GoldApiRateProvider` (`Meezan.Services/RateProviders/GoldApiRateProvider.cs`) matching this exactly: `GoldApiPriceResponse` private model with explicit `[JsonPropertyName]` attributes (camelCase JSON keys), `GetMetalRateAsync` converts `price` to per-gram via `÷31.1034768` and applies the same `XAU→GOLD`/`XAG→SILVER` translation boundary as `FrankfurterRateProvider`. Since gold-api.com has no FX endpoint, `GetFiatRatesAsync` throws `NotSupportedException` — it fulfills the shared `IRateProvider` interface (needed so `CompositeRateProvider` in sub-task 5 can treat both providers uniformly) but is intentionally metals-only, consistent with the plan's "metals-only fallback" description. Registered `builder.Services.AddHttpClient<GoldApiRateProvider>();` in `Program.cs` alongside the Frankfurter registration.
  - Verified with a scratch console app (same pattern as sub-task 2: real `ProjectReference`s, live `HttpClient`, deleted after) exercising all four paths against the actual API: `GetMetalRateAsync("XAU")` → `GOLD→USD=4343.30` (gram=139.64), `GetMetalRateAsync("XAG")` → `SILVER→USD=63.71` (gram=2.048), `GetFiatRatesAsync` → correctly threw `NotSupportedException`, `GetMetalRateAsync("BOGUS")` → correctly threw `HttpRequestException` on the live 404. `dotnet build Meezan.sln` clean, 0 errors, 49 pre-existing warnings (unrelated).
- Sub-task 4 (2026-08-08): created `RateResiliencePipeline` (`Meezan.Services/RateProviders/RateResiliencePipeline.cs`), copying `EmailResiliencePipeline`'s shape (`ResiliencePipelineBuilder` with `AddRetry` + `AddCircuitBreaker`, config from `RateIntegrationSettings.Resilience` — the same `ResilienceSettings` type reused in sub-task 1) with one deliberate deviation: `ExecuteAsync` is **generic** (`Task<T> ExecuteAsync<T>(Func<CancellationToken, Task<T>>, ...)`) rather than void, since rate-provider calls need their fetched value back (unlike fire-and-forget SMTP sends). `IsTransientFailure` classifies `HttpRequestException`/`TaskCanceledException`/`TimeoutException` as transient (matches the plan's stated classification) and `OperationCanceledException` as not (a caller-initiated cancellation shouldn't be retried). Registered as `builder.Services.AddSingleton<RateResiliencePipeline>();` in `Program.cs`, same lifetime as `EmailResiliencePipeline`. `dotnet build Meezan.sln` clean, 0 errors (warning count includes pre-existing `CS8618` nullable-init warnings on the private JSON response models from sub-tasks 2/3 — consistent with the rest of the codebase's un-annotated DTOs, not new bugs).
  - Live-verified the actual retry/circuit-breaker/classification behavior (not just compilation) with a scratch console app driving three scenarios against the real `RateResiliencePipeline`: (1) a func that throws `HttpRequestException` twice then succeeds → pipeline retried twice and returned the eventual success (`attempts=3, result=success`); (2) a func that always throws `HttpRequestException`, called repeatedly with `MinimumThroughput=2`/`FailureRatioThreshold=0.5` → circuit breaker opened (`OnOpened` fired, `IsCircuitOpen()` returned `true` afterward); (3) a func throwing `InvalidOperationException` (non-transient) → correctly attempted exactly once with no retry (`attempts=1`). Caught and fixed one test-harness-only mistake along the way (Polly rejects `MaxRetryAttempts=0` — not a pipeline bug, just an invalid test config, corrected to `1`).
- Sub-task 5 (2026-08-08): created `CompositeRateProvider` (`Meezan.Services/RateProviders/CompositeRateProvider.cs`) implementing `IRateProvider`, injecting the concrete `FrankfurterRateProvider`/`GoldApiRateProvider` (both already DI-registered via `AddHttpClient<T>()`) plus `RateIntegrationSettings`/`ILoggerFactory`. **Deliberate design decision beyond the plan's literal wording**: rather than sharing one `RateResiliencePipeline` singleton across both downstream providers (which is how sub-task 4 originally registered it in `Program.cs`), `CompositeRateProvider` constructs **two independent pipeline instances** internally — one per provider — because a shared breaker would let a GoldApi outage trip the circuit for unrelated Frankfurter calls (and vice versa), two genuinely independent failure domains. Removed the now-unused `AddSingleton<RateResiliencePipeline>()` registration and replaced it with `AddScoped<IRateProvider, CompositeRateProvider>()`. `GetFiatRatesAsync` goes through Frankfurter only (wrapped in its own pipeline) with no fallback — gold-api.com has no FX endpoint at all (confirmed in sub-task 3), so a Frankfurter fiat failure propagates as-is after retry/circuit-breaker handling. `GetMetalRateAsync` tries Frankfurter first; on any exception (post-retry/breaker), logs a warning and falls back to GoldApi through its own separate pipeline. `dotnet build Meezan.sln` clean, 0 errors, 49 warnings (all pre-existing, same set as prior sub-tasks).
  - Live-verified against the real APIs, not mocks: (1) happy path — both `GetFiatRatesAsync(["SAR","EGP"])` and `GetMetalRateAsync("XAU")` served entirely by Frankfurter (`Source="Frankfurter"` on all results); (2) genuine-failure fallback — pointed `Frankfurter.BaseUrl` at a real nonexistent DNS name (`api.frankfurter.invalid.nonexistent-domain-for-test`, actual `SocketException: No such host is known`, not a simulated exception) and confirmed `GetMetalRateAsync("XAU")` still returned a valid live gold price (`4343.30`) with `Source="GoldApi"`, proving the fallback path actually reaches the real gold-api.com API; (3) confirmed `GetFiatRatesAsync` under the same broken-Frankfurter condition correctly has no fallback and throws, matching the "GoldApi is metals-only" design.
- Sub-task 6 (2026-08-08): the DI wiring (`AddSingleton<IConnectionMultiplexer>` via `ConfigurationOptions.Parse` + `AbortOnConnectFail = false`, `AddStackExchangeRedisCache`) and `RedisHealthCheck` (`Meezan.Services/HealthChecks/RedisHealthCheck.cs`, pings via `IConnectionMultiplexer.GetDatabase()`) were already present in `Program.cs` from earlier work in this session — confirmed the packages (`StackExchange.Redis` in `Meezan.Services.csproj`, `Microsoft.Extensions.Caching.StackExchangeRedis` in `Meezan.csproj`) and registration (`.AddCheck<RedisHealthCheck>("redis", tags: ["ready"])`) were both complete and correct; this sub-task was just formalizing/verifying what existed, not new code. `AbortOnConnectFail = false` matches the repo's existing RabbitMQ resilience pattern (`RabbitConnectionManager`) — Redis unavailability at startup must never block the API from coming up, since it's a cache in front of the DB, not a system of record. `dotnet build Meezan.sln` clean, 0 errors. **Live-verified against a real local Redis** (not mocked): started the API with Redis actually running on `localhost:6379` and hit `GET /health` → `200 Healthy`, confirming `RedisHealthCheck.CheckHealthAsync`'s `PingAsync()` genuinely succeeded against a live Redis instance alongside the existing `rabbitmq`/`outbox-backlog` checks. Did not additionally test the Redis-down startup path in this pass (the code path is identical to the already-verified `RabbitConnectionManager` non-blocking pattern) — full fallback-to-DB behavior under a Redis outage is proper scope for sub-task 7 (`GetLatestAsync`'s cache-then-DB read path) and sub-task 11's verification matrix.
- Sub-task 7 (2026-08-08): added `IRateSnapshotRepository.GetLatestByPairAsync(from, to, ct)` (single index seek against the existing `(FromCurrencyCode, ToCurrencyCode, FetchedAt)` index) and `FrankfurterRateProvider.GetCurrenciesAsync` + public `FrankfurterCurrencyEntry` (`GET /v2/currencies`, live-verified shape `{iso_code, name, symbol, end_date}` — confirmed via direct `curl` before coding, matching sub-task 2's earlier discovery). Rewrote `RateService` (`Meezan.Services/RateService/RateService.cs`) as a real `BaseService`-derived implementation:
  - **`SyncAsync`**: fetches `GetFiatRatesAsync` for every currently-known non-USD fiat code + `GetMetalRateAsync("XAU"/"XAG")` via the injected `IRateProvider` (the already-resilient `CompositeRateProvider`), inserts every quote as a new append-only `RateSnapshot` row (`Rate = RatePerGram ?? RatePerUnit` — metals store the per-gram figure, fiat the per-unit figure) inside one `ExecuteInTransactionAsync`, alongside an **insert-only currency sync**: calls `GetCurrenciesAsync`, filters to currencies whose `end_date` is today-or-later (Frankfurter's list includes ~80 historical/decommissioned codes with a past `end_date` — seeding those would pollute the lookup table with currencies that can never get a rate), skips any code already present (never overwrites the hand-authored USD/SAR/EGP seed rows), and upserts the rest with a small static EN→AR name dictionary (falls back to the English name otherwise, per the plan's "content follow-up, not a blocker"). After the DB commit, refreshes one Redis key per stored pair (`rate:{from}:{to}`, 48h TTL as a safety net against a missed sync — not the primary freshness mechanism, `SyncAsync` overwrites these daily).
  - **`GetLatestAsync`**: same-currency shortcut (1m) unchanged from the placeholder; otherwise a `ResolveDirectOrInverseAsync(from, to)` helper (direct stored snapshot, else `1/inverse` snapshot) is reused three ways — once for the top-level pair, and once per leg of the cross-through-USD case (`from→USD` and `USD→to`) when neither side is already USD — which reproduces the plan's exact worked examples (`EGP→SAR = (1/USD→EGP) × USD→SAR`, `SAR→GOLD = (1/USD→SAR) × (1/GOLD→USD)`) from one small piece of reused logic rather than three separate code paths. Throws `RatesUnavailableException` (unchanged exception, still unlocalized — sub-task 10 adds the message) only when no combination of stored snapshots resolves the pair.
  - `IRateService.SyncAsync` added to the interface (was `GetLatestAsync`-only). `RateService`'s constructor now also takes the concrete `FrankfurterRateProvider` (for currency-sync, mirroring how `CompositeRateProvider` injects concrete providers) and `IDistributedCache`/`ILogger<RateService>` — no DI registration changes needed, `AddScoped<IRateService, RateService.RateService>()` already resolves the new dependencies since `IRateProvider`/`FrankfurterRateProvider`/`IDistributedCache` were all wired in sub-tasks 1–6. `dotnet build Meezan.sln` clean, 0 errors.
  - **Live-verification caught a real bug**: my first cut called `Cache.GetStringAsync`/`SetStringAsync` directly and unwrapped. Testing against a genuinely-down local Redis (not simulated — `Test-NetConnection localhost:6379` failing) showed `SyncAsync` threw an unhandled `RedisConnectionException` from the cache-refresh loop, and `GetLatestAsync` would have done the same from `GetStoredRateAsync` — directly contradicting BR-19's "Redis unavailability must never block" and defeating the entire point of the cache-aside DB-fallback design (`AbortOnConnectFail=false` from sub-task 6 only covers the multiplexer's *startup*; individual commands still throw while genuinely disconnected). Fixed by wrapping every Redis read/write in `TryCacheGetAsync`/`TryCacheSetAsync` (log a warning, degrade to null/no-op) so a Redis outage now falls through to the DB transparently instead of propagating.
  - **Re-verified end-to-end against a real, still-down Redis** (not mocked) after the fix: ran the full flow (`SyncAsync` then a battery of `GetLatestAsync` calls) via a temporary hook in `Program.cs`'s existing startup-scope block, reverted immediately after (no trace left — `git diff Program.cs` confirmed only sub-task 6's prior changes remain). Every Redis call correctly logged a warning and fell through; `SyncAsync` completed successfully (`85 snapshots persisted, 0 new currencies discovered` on this run — see below); results, all mathematically self-consistent: `USD→SAR=3.75`, `SAR→USD=0.266666...=1/3.75`, `EGP→SAR=0.075320...` (cross-through-USD), `SAR→GOLD=0.001949...`/`GOLD→SAR=513.04...` (reciprocal of each other, confirming the cross-metal path), `GOLD→SILVER=68.89` (matches the live `GOLD→USD`/`SILVER→USD` price ratio), `SAR→SAR=1` (same-currency), and `SAR→ZZZ` (nonexistent code) correctly threw `RatesUnavailableException`. Cross-checked directly against the DB via `sqlcmd`: 89 total `RateSnapshot` rows and 86 `Currency` rows (up from the original 5) persisted across this run and an earlier one that failed only at the (pre-fix) cache step — confirming the DB transaction genuinely commits *before* the cache-refresh loop runs, so a cache failure never loses the fetched data. Also incidentally validated the "currency list grows and feeds future syncs" design end-to-end: the earlier pre-fix run's currency-sync grew the table from 5→86 rows, and this run's `fiatQuoteCodes` picked up all of them automatically (85 quotes fetched vs. an initial run's 4), with zero manual wiring between the two. (Arabic names in the `sqlcmd` console output showed as `?` — confirmed via the pre-existing, known-good seeded `SAR` Arabic name showing identically as `?`, proving it's the terminal's codepage, not a data/encoding bug; columns are `nvarchar(max)`.) Left the synced data in place — it's exactly what the real daily job (sub-task 8) will produce, not throwaway test junk.

- Sub-task 8 (2026-08-08): added `IJobService.SyncRates()` and its `JobService` implementation (`RateService.SyncAsync().GetAwaiter().GetResult()` — the identical `void`-wrapper shape as `CleanupExpiredRefreshTokens`, `JobService` now also takes `IRateService` in its constructor). Registered in `Program.cs`'s existing startup-scope block, right after the `cleanup-refresh-tokens` registration: `recurringJobManager.AddOrUpdate<IJobService>("rate-sync", js => js.SyncRates(), rateIntegrationSettings.CronExpression)` (the raw cron string from sub-task 1's settings, not a `Cron.*` helper, since it's config-driven) plus `BackgroundJob.Enqueue<IJobService>(js => js.SyncRates())` for the immediate one-off run. `dotnet build Meezan.sln` clean, 0 errors.
  - **Live-verified the actual Hangfire wiring, not just compilation**: started the API and, within seconds, confirmed via direct DB queries (not the dashboard) that both halves fired correctly. (1) **Startup fire-and-forget**: `RateSnapshot` row count went from 89 → 174 (`sqlcmd` before/after) with a fresh `MAX(FetchedAt)` timestamp matching the exact moment of this run — the enqueued job executed within seconds of startup, comfortably inside the plan's "within minutes" bar for a fresh install. (2) **Recurring registration**: queried `HangfireOperation`'s `[HangFire].[Hash]` table directly for `recurring-job:rate-sync` and confirmed every field is correct — `Cron = "0 2 * * *"` (sub-task 1's default, sourced from `appsettings.json` at runtime, not hardcoded), `Job` targets `IJobService.SyncRates`, `NextExecution = 2026-08-09T02:00:00Z` (correctly the next 02:00 UTC from "now"), `TimeZoneId = UTC`. Redis was genuinely down for this whole run (consistent with sub-task 7's fix) — the sync still completed and persisted correctly, degrading only the cache-refresh step exactly as designed.

- Sub-task 9 (2026-08-08): added `RateDto {QuoteCurrencyCode, Rate}` / `RatesResponseDto {BaseCurrencyCode, List<RateDto> Rates}` (`Meezan.Dto/DTOs/Rate/`), `IRateService.GetLatestQuotesAsync(baseCurrencyCode, List<string> quoteCurrencyCodes, ct)` (thin loop over the existing `GetLatestAsync` per requested quote — kept on `RateService` rather than the controller so the controller stays a one-liner, matching this codebase's "no business logic in controllers" convention; validates non-empty `base`/`quotes` via `InvalidRequestException`), and `RatesController` (`GET /api/rates/latest?base=&quotes=`, `[Authorize]`, `quotes` parsed from a comma-separated query string). Deliberately did **not** add a `FetchedAt` field to `RatesResponseDto` in this sub-task — the plan attributes that specifically to sub-task 10 ("`RatesUnavailable`... surfaced as a `FetchedAt` field on the response DTO"), bundled together with its localization key as one cohesive follow-up rather than guessed at here. `dotnet build Meezan.sln` clean, 0 errors.
  - **Live-verified over real HTTP**, not just unit-level: registered a throwaway user, logged in for a real JWT, and hit the live endpoint through the full auth pipeline: multi-quote happy path (`base=SAR&quotes=USD,GOLD,EGP` → 200, all three rates correct), a plain inverse pair (`USD→SAR=3.75`), a metal-based base (`base=GOLD&quotes=SILVER,USD` → 200, correct), same-currency (`rate=1`), a missing `quotes` param (→ 400 RFC7807 problem+json via `[ApiController]`'s built-in required-parameter validation, matching spec §6's "RFC 7807 problem+json for errors" without any code of my own), an unresolvable quote code (`ZZZ` → 503 `"Rates unavailable"`, the existing `RatesUnavailableException` mapping), and no `Authorization` header (→ 401). All 7 cases behaved exactly as expected. Redis was down for this whole run too — responses succeeded but were noticeably slower (multiple ~5s Redis connection timeouts per resolved pair, since a single `GetLatestAsync` call can make several `GetStoredRateAsync` round-trips) — correctness held throughout (BR-19's "never blocks"), but a prolonged Redis outage would degrade *latency* under this design, not just cache-hit-rate; flagging this as a known tradeoff rather than fixing it now, since BR-19 specifies no blocking, not a latency SLA, and fixing it (e.g. tuning `ConnectTimeout`down) is an infrastructure concern outside this sub-task's scope. Deleted the throwaway test user (`User`/`RefreshToken` rows) afterward — this was disposable auth-flow scaffolding, unlike sub-task 7/8's synced rate data which is real production output.

- Sub-task 10 (2026-08-08): added the `RatesUnavailable` localization key (3-file wiring: `localizationFile.json` en/ar, `ILocalizationService`, `LocalizationService` — same pattern as every other message this session) and used it at both `RatesUnavailableException` throw sites in `RateService` (previously the exception's hardcoded default `"Rates unavailable"` string — now genuinely localized via `Accept-Language`). Added `RateDto.FetchedAt` (always populated, not just on failure — per BR-19's "displayed as rates as of {fetchedAt}," every rate the frontend shows needs this caption, not only a stale one) and a new private `GetLatestWithFetchedAtAsync`/`ResolveDirectOrInverseWithFetchedAtAsync` pair mirroring `GetLatestAsync`'s exact same-currency/direct-or-inverse/cross-through-USD structure, used only by `GetLatestQuotesAsync`. **Deliberately DB-only, bypassing Redis**: the cache-aside hot path's Redis values are bare rate strings with no timestamp, and reshaping that format to carry `FetchedAt` risked regressing sub-task 7/9's already-verified `GetLatestAsync`/`AccountService`/`TransactionService` behavior for a comparatively low-traffic controller endpoint — a deliberate, documented duplication of the resolution *shape* (not logic on a shared cache) rather than a risky rework of the hot path's storage format. Same-currency rates get `FetchedAt = DateTime.UtcNow` (exact by definition, not snapshot-derived); a cross-through-USD rate's `FetchedAt` is the **older** of its two legs (the binding freshness constraint on the computed value). `dotnet build Meezan.sln` clean, 0 errors.
  - **Live-verified over real HTTP** (register/login/call, same pattern as sub-task 9, throwaway user deleted after): `GET /api/rates/latest?base=SAR&quotes=USD,GOLD,EGP,SAR` → each of the three snapshot-derived rates carried the identical `fetchedAt` (the last sync's timestamp), while the `SAR→SAR` same-currency entry correctly carried a distinct, later `fetchedAt` (the moment of the request itself). Confirmed the localization actually switches on `Accept-Language`: an unresolvable quote (`ZZZ`) returned `"Exchange rates are not available yet. Please try again shortly."` (503, default/English) and, with `Accept-Language: ar`, `"أسعار الصرف غير متوفرة حاليًا. يرجى المحاولة مرة أخرى بعد قليل."` (503) — the same request, same failure, genuinely different body driven by the header.

- Sub-task 11 (2026-08-08): consolidated manual verification of every item in the plan's sub-task 11 checklist, most of which had already been live-verified as a byproduct of building sub-tasks 5–10 (re-summarized here per-item, with fresh evidence added where a gap remained) — no code changes, verification only:
  - **Cold-cache DB fallback**: verified repeatedly across sub-tasks 7–10 with Redis genuinely unreachable the entire time — `GetLatestAsync`/`SyncAsync`/`RatesController` all continued to resolve correct rates entirely from the DB, with every Redis call degrading to a logged warning (the bug caught and fixed in sub-task 7).
  - **Redis hit path** (the one gap not yet directly observed with a *warm* cache): had the user start Redis via WSL (`sudo systemctl start redis-server`), confirmed via `redis-cli` that `SyncAsync` populates real hash entries (`rate:USD:SAR` → `{data: "3.75", absexp: ..., sldexp: -1}`, `Microsoft.Extensions.Caching.StackExchangeRedis`'s actual on-wire format — explains why the earlier Redis-down logs showed `command=HMSET`, not `SET`) with the correct ~48h TTL. Then, via a temporary timed hook in `Program.cs` (added, exercised, and fully reverted afterward — `git diff Program.cs` confirms no trace), called the hot `GetLatestAsync("USD","SAR")` path three times against the warm cache: `29ms`, `0ms`, `0ms` — a dramatic, unambiguous contrast with the multi-second-to-tens-of-seconds-per-call latency observed throughout sub-tasks 7–10 under a Redis outage, confirming the cache-hit path is genuinely taken (not silently falling through to the DB every time).
  - **Forced-failure→GoldApi fallback**: verified live in sub-task 5 (pointed `Frankfurter.BaseUrl` at a real nonexistent DNS name, confirmed `CompositeRateProvider.GetMetalRateAsync` still returned a live gold-api.com price with `Source="GoldApi"`).
  - **Circuit-breaker-open behavior**: verified live in sub-task 4 against the real `RateResiliencePipeline` (repeated failures opened the breaker, `OnOpened` fired, `IsCircuitOpen()` reported `true`).
  - **Append-only, never-updates**: `RateSnapshotRepository`/`RateService` never call `Update`/`Delete` on `RateSnapshot` — by construction, not just by observation. Confirmed empirically too: the row count strictly increased across every one of this session's app runs with no decreases (89 → 174 → ... → 854 across sub-tasks 7–11), consistent with pure appends.
  - **`GetLatestAsync` direct/inverse/cross-through-USD for EGP→SAR and SAR→GOLD**: verified live in sub-task 7 with exactly these two pairs, both mathematically cross-checked against their reciprocals/component legs.
  - **Currency-sync upserts without duplicating**: verified across sub-tasks 7 (5→86 rows on first sync) and every run since (7 additional full syncs, each logging `0 new currencies discovered` and leaving the `Currency` table at a stable 86 rows).
  - **Fresh-install startup sync populates usable rates within minutes**: verified live in sub-task 8 (89→174 `RateSnapshot` rows within seconds of app startup, driven purely by the `BackgroundJob.Enqueue` fire-and-forget call) — not tested against a literally-empty table in this pass (the existing dev DB already has real synced history built up over this session, and truncating genuinely reconstructible-but-still-real data for a check that doesn't exercise a different code path — `SyncAsync` has no branch on whether the table started empty — wasn't judged worth the trade-off; sub-task 7's very first sync run, against what *was* an empty table at the time, plus sub-task 8's proof of the job-trigger mechanism, together already cover this).
  - `dotnet build Meezan.sln` clean, 0 errors throughout. Deleted the one throwaway test user created for this pass afterward.
- **Phase 010 complete.** Full rate-integration architecture (adapters, composite fallback, resilience, scheduled + startup sync, Redis cache-aside with graceful degradation, append-only snapshots, direct/inverse/cross-through-USD resolution, API contract row #24) implemented and live-verified end-to-end, including one real bug (unhandled Redis exceptions breaking the BR-19 "never blocks" guarantee) caught and fixed by this session's live-verification discipline.

## Approval Log

| Sub-Task | Approved By | Date       |
| -------- | ----------- | ---------- |
| Plan     | User        | 2026-08-07 |
